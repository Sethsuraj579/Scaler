"""
OpenEnv GridNav Test Suite

Run tests with:
    pytest
    pytest -v           (verbose)
    pytest -k test_name (specific test)
    pytest --tb=short   (short tracebacks)
    pytest -m unit      (specific marker)
"""
